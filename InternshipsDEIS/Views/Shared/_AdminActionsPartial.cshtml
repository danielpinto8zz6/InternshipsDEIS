@using Microsoft.AspNetCore.Identity
@using InternshipsDEIS.Models

@if (User.Identity.IsAuthenticated && User.IsInRole("Administrator"))
{
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link text-dark" asp-controller="User" asp-action="Index">Users</a>
        </li>
    </ul>
    <ul class="navbar-nav">
        <li class="nav-item dropdown">
            <a class="nav-link text-dark dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
                aria-haspopup="true" aria-expanded="false">
                Candidatures
            </a>
            <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                <a class="dropdown-item" asp-controller="InternshipCandidature" asp-action="Index">Internships</a>
                <a class="dropdown-item" asp-controller="ProjectCandidature" asp-action="Index">Projects</a>
            </div>
        </li>
    </ul>
}