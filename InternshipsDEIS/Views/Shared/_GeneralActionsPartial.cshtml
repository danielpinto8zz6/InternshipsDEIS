@using Microsoft.AspNetCore.Identity
@using InternshipsDEIS.Models

@if (!User.Identity.IsAuthenticated || (User.Identity.IsAuthenticated && User.IsInRole("Administrator")))
{
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link text-dark" asp-controller="Internship" asp-action="Index">Internships</a>
        </li>
    </ul>
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link text-dark" asp-controller="Project" asp-action="Index">Projects</a>
        </li>
    </ul>
}