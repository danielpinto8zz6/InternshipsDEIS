@using Microsoft.AspNetCore.Identity
@using stagesDEIS.Models

@if (User.Identity.IsAuthenticated && User.IsInRole("Student"))
{
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link text-dark" asp-controller="Candidature" asp-action="Index">Candidatures</a>
        </li>
    </ul>
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link text-dark" asp-controller="Proposal" asp-action="Index">Proposals</a>
        </li>
    </ul>
}